#include "game/server/worldsession.h"

void WorldSession::HandleScreenInfo(QString& /*packet*/)
{
    /*
    quint16 screenWidth, screenHeight;
    bool fullScreen;

    QStringList datas = packet.mid(2).split(";");

    screenWidth = (quint16)datas.at(0).toUInt();
    screenHeight = (quint16)datas.at(1).toUInt();
    fullScreen = (bool)(datas.at(2) == "1");
    */
}
